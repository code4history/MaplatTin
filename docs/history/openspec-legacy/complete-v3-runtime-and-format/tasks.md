## 1. Specification
- [ ] 1.1 Define the V3 editing payload contract, including which fields are preserved for re-editing.
- [ ] 1.2 Define how optional GCP metadata is serialized without breaking existing point-indexed triangulation.
- [ ] 1.3 Define the V3 distribution payload contract for runtime-only use.
- [ ] 1.4 Define compatibility and migration rules between compiled editing payloads and runtime distribution payloads.
- [ ] 1.5 Define the V3.1 development-stage rule that `@maplat/tin` owns both format generation and single-coordinate evaluation until the payload contract is fixed.
- [ ] 1.6 Define V3.1 initializer semantics for `gcps` plus optional `crs`, including `crs: true`.
- [ ] 1.7 Define the unresolved map-CRS state in which triangulation is deferred until CRS and world-file parameters are fixed.
- [ ] 1.8 Define the explicit CRS-estimation procedure from GCPs, including candidate CRS enumeration and world-file fitting.
- [ ] 1.9 Define the post-fix option that keeps GCPs as CRS-control points only without using them as triangulation points.
- [ ] 1.10 Define staged world-file fitting: candidate scoring with isotropic scale, rotation, and translation, followed by post-selection shear optimization.
- [ ] 1.11 Define debug-only estimation diagnostics, including candidate scores and pre/post-shear residual reporting.
- [ ] 1.12 Define the explicit estimation entry point, such as `estimateMapCrs()`, and the smaller reusable helper methods beneath it.

## 2. Planning
- [ ] 2.1 Confirm which fields remain owned by `@maplat/tin` and which are consumed but not generated by `@maplat/transform`.
- [ ] 2.2 Decide whether the initial implementation ships canonical JSON only, with binary packaging deferred.
- [ ] 2.3 Enumerate fixture updates needed for V3 editing payloads and distribution payloads.
- [ ] 2.4 Define end-to-end demos that compare V2, V3, and V3.1 from payload generation through evaluation.
- [ ] 2.5 Define the migration checkpoint at which single-coordinate evaluation moves from `@maplat/tin` to `@maplat/transform`.
- [ ] 2.6 Prepare Japanese candidate CRS sets for the initial estimator scope: `3857`, WGS84 UTM, WGS84 JGD/TM-style Japan zones, Tokyo datum UTM, and Tokyo datum plane-rectangular zones.
- [ ] 2.7 Decide how the staged fitter reports both the pre-shear and post-shear residuals for debugging and evaluation demos.
- [ ] 2.8 Define the debug flag behavior for retaining or omitting estimation diagnostics from working state and serialized output.
- [ ] 2.9 Define unit-test boundaries for helper methods such as candidate enumeration and `world_file_basic` fitting.

## 3. Validation
- [ ] 3.1 Review the proposal with `@maplat/transform` so both packages agree on runtime-required fields.
- [ ] 3.2 Validate this change with `openspec validate complete-v3-runtime-and-format --strict`.
